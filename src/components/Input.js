import React from 'react';

const Input = (props) => {
    const {onchange,placeholder}=props;
    return (
        <div>
            <input type="text" />
        </div>
    );
}

export default Input;
